% % ============================================================================
% \chapter{Reproducibility: figure and table map}\label{app:repro}
% % ============================================================================

% Every number and figure in this manuscript is produced by one of five notebooks; captions
% name the source figure verbatim. Summary map:

% \begin{itemize}
% \item \textbf{NB01} (\texttt{01\_data\_cleaning}): quote-count distributions; cleaned quote
% cloud; forward/parity diagnostics. Output: \texttt{option\_prices\_clean.parquet}.
% \item \textbf{NB02} (\texttt{02\_svi\_ssvi\_benchmark}): Vogt exhibit (Fig.~``Vogt SVI: w,
% Durrleman g, implied density''); JW conversions and repair; SSVI certificate table;
% interpolation; day smiles; bucket figure; domain figure; benchmark tables
% (Tabs.~\ref{tab:nb02}--\ref{tab:buckets}). Outputs:
% \texttt{benchmark\_svi\_slices\_full.parquet}, \texttt{benchmark\_ssvi\_days\_full.parquet}.
% \item \textbf{NB03} (\texttt{03\_deep\_smoother\_v8}): AD validations; GJ certificates;
% equal-epoch dynamics (Tab.~\ref{tab:ablation}); stress maps, arbitration-at-$k{=}0$, gate and
% $\lambda$-sweep logs (Tab.~\ref{tab:sweep}); sparsity two-panel; real-day
% smiles/surface/audit; penalty-ablation quadrant. Outputs:
% \texttt{deep\_smoother\_days.parquet}, \texttt{nb03\_surfaces/*.npz} (incl.\ the
% $\lambda{=}0$ counterfactual).
% \item \textbf{NB04} (\texttt{04\_neural\_operator\_v2}): training curves; transfer bar
% chart; gated invariance; latency table; per-day regime table; operator summary
% (Tab.~\ref{tab:opaudit}); unseen-day fit. Outputs: \texttt{nb04\_operator\_run/*.parquet},
% \texttt{nb04\_surfaces/*.npz}.
% \item \textbf{NB05} (\texttt{05\_downstream\_economics}): FD-field validation; local-vol
% heatmaps with repaired cells (Tab.~\ref{tab:localvol}); BL densities
% (Tab.~\ref{tab:density}); static-arbitrage scanner (Section~\ref{sec:scanner}); residual
% economics and the F2 backtest incl.\ the clean-vs-dirty counterfactual
% (Tab.~\ref{tab:f2}); the detection-power frontier (Section~\ref{sec:power}); VIX
% replication (Tab.~\ref{tab:vix}). Outputs: \texttt{nb05\_local\_vol.parquet},
% \texttt{nb05\_densities.parquet}, \texttt{nb05\_residual\_economics.parquet},
% \texttt{nb05\_static\_arb.parquet}, \texttt{nb05\_backtest\_daily.parquet},
% \texttt{nb05\_backtest\_stats.parquet}, \texttt{nb05\_detection\_frontier.parquet},
% \texttt{nb05\_vix.parquet}.
% \end{itemize}

% \noindent The draft figures generated for Chapter~\ref{ch:theory}
% (Figures~\ref{fig:vogt}--\ref{fig:oparch}) are produced by \texttt{make\_figures.py} from the
% closed forms of the text; they are exact but tagged \draftnote{[draft --- \`a valider]} until
% replaced or confirmed against the notebook versions.

% \noindent Production checklist: NB03 with \texttt{LIMIT\_DATES=None},
% \texttt{EXPORT\_ALL=True} (parallelise by year, $\sim$30\,h CPU); NB04 on GPU with raised
% epochs and \texttt{NB04\_EXPORT\_DAYS} covering the NB03 dates; re-run NB05; replace all
% entries marked $^\dagger$.
